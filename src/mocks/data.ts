import { factory, primaryKey } from '@mswjs/data';

export const db = factory({
  model1: {
    id: primaryKey(String),
  },
  model2: {
    id: primaryKey(String),
  },
  model3: {
    id: primaryKey(String),
  },
  model4: {
    id: primaryKey(String),
  },
  model5: {
    id: primaryKey(String),
  },
  model6: {
    id: primaryKey(String),
  },
  model7: {
    id: primaryKey(String),
  },
  model8: {
    id: primaryKey(String),
  },
  model9: {
    id: primaryKey(String),
  },
  model10: {
    id: primaryKey(String),
  },
  model11: {
    id: primaryKey(String),
  },
  model12: {
    id: primaryKey(String),
  },
});

export default db;
